export default class UserGroupInfo {
    mneoServerLite: Info
}

class Info {
    bassAddress: string
    port: string
    contextPath: string
    applicationPath: string
    defaultContentType: string
}